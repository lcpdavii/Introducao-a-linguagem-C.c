#include <stdio.h>

//a unica funcao que ira ser executada sera a main, se escrever algum codigo que estiver fora da main, ira dar erro no compilador, ou seja, o programa so ira executar o que estiver dentro da MAIN -> significa PRINCIPAL. Um progrma C so pode ser considerado um programa se ele tiver a main.
int main(){
  printf("Ola mundo.\n");

  return 0;
}